import 'package:flutter/material.dart';
//class containing the colours used in the application
class ColourScheme {
	static Color primaryColor = Color.fromRGBO(90, 150, 188, 1);
	static Color secondaryColor = Color.fromARGB(255, 0, 0, 0);
	static Color accentColor = Color.fromRGBO(255, 180, 63, 63);
}